variables:
  PROJECT_PREFIX: mrtolerant.ru

stages:
  - build
  - test
  - run

Build:
  stage: build
  script:
    - docker-compose -f docker/docker-compose.yml -p ${PROJECT_PREFIX}.${CI_COMMIT_REF_SLUG} build
  tags:
    - bash

# Test:
#   stage: test
#   script:
#     - docker run  --entrypoint=/bin/sh docker_node -c 'yarn test'
#   tags:
#     - bash
#   only:
#     - merge_requests

Run:
  stage: run
  script:
    docker-compose -f docker/docker-compose.yml -p ${PROJECT_PREFIX}.${CI_COMMIT_REF_SLUG}  up -d
  tags:
    - bash