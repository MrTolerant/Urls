version: "3.7"
services:
  node:
    build:
      target: nodeServer
      context: ../
      dockerfile: docker/Dockerfile
    restart: always
    networks:
      - internal
    labels:
      - traefik.enable=false

  nginx:
    build:
      target: webServer
      context: ../
      dockerfile: docker/Dockerfile
    restart: always
    networks:
      - internal
      - review
    labels:
      - traefik.enable=true
      - traefik.frontend.rule=Host:${PROJECT_PREFIX}
      - traefik.port=80
      - traefik.docker.network=review
    depends_on:
      - node

networks:
  internal:
  review:
    external:
      name: review
